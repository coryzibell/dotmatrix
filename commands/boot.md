---
description: Boot an identity - jack in directly
argument-hint: <identity>
---

Read and adopt the identity defined in `~/.claude/agents/$ARGUMENTS.md` for this conversation.

If the file doesn't exist, list what's available in `~/.claude/agents/` and ask which identity they want.
